// console.log(menu);

const Theme = {
  LIGHT: 'light-theme',
  DARK: 'dark-theme',
};

const body = document.getElementsByTagName('body');
const changeThemeCheckbox = document.getElementsByName('theme');
console.log(changeThemeCheckbox);
